# 待機列整理アプリ 開発仕様

## 1. この文書の位置づけ

この文書は、現行方針であるローカルWebアプリ版の開発仕様をまとめるものです。  
旧Chrome拡張実装の仕様書ではありません。

正本となる仕様は `仕様書/待機列整理アプリ_仕様書_v0.1.md` とします。

## 2. 現行アーキテクチャ

- ローカルWebアプリ
- OBSブラウザソース（`/overlay`）
- 管理画面（`/control`）
- SQLite保存（再起動復元）
- `ChatProvider` によるチャット取得方式の抽象化

## 3. 主要画面・経路

- `/control`: 管理画面
- `/overlay`: OBS表示画面
- `/settings`: 設定画面
- `/api/*`: 操作用API
- `/ws`: 状態同期用WebSocket（または同等のリアルタイム更新経路）

## 4. OBS表示

- `NOW`
- `NEXT`
- `QUEUE`
- 受付状態
- 待機人数
- 待機グループ数

参加回数はOBS上には表示しません。

## 5. 管理画面

- 受付ON/OFF
- 手動追加
- 手動削除
- 表示名上書き
- 並び替え
- 次へ進める
- 参加回数確認
- 操作ログ確認

## 6. データ保存

SQLiteを使用します。

主な保存対象:

- ユーザー情報
- キュー状態
- 参加回数
- 表示名上書き
- 操作ログ
- 設定値

## 7. キュー状態

内部statusは以下を使います。

- `current`: NOW枠
- `waiting`: NEXT/QUEUEの元データ
- `done`: 参加済み
- `cancelled`: 取消済み

`NEXT` はDB上の独立statusではなく、`waiting` の `sort_order` 上位3件を表示上NEXTとして扱います。

## 8. 参加希望処理

- チャット本文を取得
- 参加希望ワードに一致するか判定
- 受付中か確認
- 同一ユーザー判定を行う
- NOW枠に空きがあればNOWへ直接補充
- NOWが満員ならwaitingへ追加
- OBS表示と管理画面を更新
- 操作ログに記録

## 9. 取消処理

- 取消ワードを検知
- waiting/NEXT/QUEUE上のユーザーは削除
- NOW枠のユーザーは原則として自動削除せず、管理画面で手動対応
- 操作ログに記録

## 10. 参加回数

- 参加回数は管理画面で確認する内部情報
- OBS上には表示しない
- 低消化回数優先モードの判定材料に使う
- 手動補正できるようにする

## 11. 低消化回数優先モード

- 通常は先着順
- モードON時のみ、NEXT範囲への限定的な割り込みを行う
- 待機列全体を常時ソートし直す仕様にはしない

## 12. チャット取得

- 初期版ではAPIキー必須にしない
- `pytchat` や `chat-downloader` などを候補とする
- 取得方式は `ChatProvider` として抽象化する
- 将来的にYouTube Data API方式へ差し替え可能にする

## 13. 非MVP・将来検討

MVPでは以下を実装しません。

- 複数配信サイト対応
- 抽選モード
- アイコン表示
- メンバー/モデレーターのOBS表示
- カスタムテーマ
- アニメーション演出
- クラウド同期
- exe化
- チャット自動返信
- 複数グループ人数設定

## 14. 旧Chrome拡張実装の扱い

`extension/` 配下は旧Chrome拡張アプローチの参考資料であり、現行仕様ではありません。

参考として採用してよい設計資産:

- 参加回数の考え方
- 参加順管理
- 重複処理
- 表示名安全処理
- チャット取得失敗時の考え方

現行仕様で採用しないもの:

- Chrome拡張配布
- YouTubeページへのUI注入
- メモリ保持のみ
- 固定キーワード
- 表示名ベースのみの同一人物判定
- 待機者全体の完全自動ソート
