# 実配信前テストチェックリスト

このチェックリストは、待機列整理アプリを実配信で使う前に、MVPの主要導線が配信運用に耐えるかを確認するためのものです。

## 0. 前提

- 検証は本番配信ではなく、限定公開・非公開・テスト枠などの安全な環境で行う。
- OBSには `/overlay` をブラウザソースとして追加できる状態にしておく。
- 配信者操作用に `/control` をブラウザ、または OBS カスタムブラウザドックで開ける状態にしておく。
- 必要に応じてテスト前に `data/waiting_list.sqlite3` を退避する。

## 1. 起動確認

- [ ] Windowsで `start.bat` をダブルクリックして起動できる。
- [ ] 初回起動時に `.venv` 作成と依存インストールが完了する。
- [ ] `http://127.0.0.1:8080/control` が自動で開く。
- [ ] `/control`、`/overlay`、`/settings` がブラウザで表示できる。
- [ ] 起動ログに明らかな例外が出ていない。

## 2. 管理画面 `/control`

- [ ] 受付開始/終了を切り替えられる。
- [ ] 低消化回数優先モードを切り替えられる。
- [ ] 手動追加で待機列に参加者を追加できる。
- [ ] 待機列の削除、末尾移動、並び替えができる。
- [ ] 申告プレイヤーネームの編集と削除ができる。
- [ ] 「次の対戦に移る（参加回数+1）」でNOW参加者の参加回数が増える。
- [ ] リセットで受付状態、待機列、参加回数が想定どおり初期化される。

## 3. OBS表示 `/overlay`

- [ ] OBSブラウザソースで `http://127.0.0.1:8080/overlay` を表示できる。
- [ ] NOW / NEXT / QUEUE が現在状態に追従する。
- [ ] 長い表示名が1行で省略され、画面崩れしない。
- [ ] 参加者不足時に「参加者募集中」が控えめに表示される。
- [ ] `/overlay` に操作ボタンや入力欄が表示されない。
- [ ] `/control` の操作後、OBS側表示が実用上問題ない速度で更新される。

## 4. 設定画面 `/settings`

- [ ] 申告プレイヤーネームをOBSに表示する設定をON/OFFできる。
- [ ] ON時、OBS表示に `表示名（申告名）` が出る。
- [ ] OFF時、OBS表示には通常の表示名だけが出る。
- [ ] `/api/state` 取得失敗時の「再読み込み」が設定値をトグルしない。
- [ ] 参加希望ワード、参加辞退ワード、グループ人数、40秒ロックが固定仕様として読める。

## 5. コメント投入と待機列反映

- [ ] `参加希望` を含むコメントで参加希望として扱われる。
- [ ] `参加希望 名前 たなかたろう` で申告プレイヤーネームが保存される。
- [ ] `参加希望者` と `参加希望順` は参加希望として扱われない。
- [ ] `参加辞退` と `参加を辞退` で待機列から削除される。
- [ ] `キャンセル`、`取消`、`やめます`、`cancel` は辞退として扱われない。
- [ ] 同じ `externalMessageId` の重複コメントは二重反映されない。
- [ ] 同一ユーザーの再 `参加希望` は現在位置を捨てて最後尾に並び直す。

## 6. 40秒状態変更ロック

- [ ] コメント由来の `join` / `cancel` / `rejoin` 後、同一ユーザーの次の状態変更が40秒間無視される。
- [ ] ロック中の申告名更新が反映されない。
- [ ] `/control` からの手動削除、並び替え、申告名編集は40秒ロックの対象外になる。
- [ ] 40秒経過後、同一ユーザーの状態変更が再び反映される。

## 7. SQLite復元

- [ ] 待機列、受付状態、低消化回数優先、表示設定、参加回数を作った状態でアプリを停止する。
- [ ] 再起動後、保存対象の状態が復元される。
- [ ] コメント本文、`externalMessageId`、`userKey` 生値が長期保存されない方針と矛盾しない。
- [ ] 壊れたDBや退避DBがある場合の復旧手順を運用者が説明できる。

## 8. 外部チャット取得アプリ連携

- [ ] 外部アプリから `POST /api/comments/receive` にコメントを送れる。
- [ ] 受信payloadの `displayName`、`userKey`、`message`、`externalMessageId` が想定どおり渡る。
- [ ] 外部アプリ連携は公式に許可された方法のみを使っている。
- [ ] 外部アプリ本体の改造、内部DB参照、非公開API利用、通信解析をしていない。

## 9. 配信リハーサル

- [ ] 配信者が `/control` の主要操作を迷わず実行できる。
- [ ] OBS上で `/overlay` が読みやすいサイズと位置になっている。
- [ ] 受付開始から参加、辞退、次の対戦、リセットまで一連の流れを通せる。
- [ ] 想定外のコメントが来ても、待機列が大きく崩れない。
- [ ] 失敗時に、アプリを使わず手動運用へ戻す判断基準を決めている。

## 10. 判定

- [ ] 重大な表示崩れがない。
- [ ] 待機列がコメントと管理操作に一貫して追従する。
- [ ] 参加回数と低消化回数優先が期待どおり動く。
- [ ] OBSに出してはいけない内部情報が表示されない。
- [ ] 配信者が当日の運用手順と退避手順を把握している。

全項目が完了しない場合でも、未完了項目が配信事故につながるかを判断し、必要なら配信本番投入を延期する。
