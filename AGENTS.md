# 開発ルール

## 現行方針

- アプリ仮称は「待機列整理アプリ」
- Chrome拡張ではなくローカルWebアプリとして開発する
- OBS表示は `/overlay`
- 管理画面は `/control`
- 設定画面は `/settings`
- 視聴者3人固定
- 状態保存はSQLiteを前提とする
- チャット取得は外部チャット取得アプリケーションから受け取る
- 本アプリは、受信コメントの正規化、参加/取消判定、待機列整理、管理画面、OBS表示を担当する

## 旧実装の扱い

- `extension/` 配下は旧Chrome拡張アプローチの参考資料
- `extension/` 配下を現行実装として扱わない
- 旧実装から参考にしてよいのは、参加回数、参加順、重複処理、表示名安全処理などの設計思想のみ

## MVPで実装しないもの

- Chrome拡張
- YouTubeページへのUI注入
- 配信サイトAPIへの直接接続
- 非公開API利用
- 外部アプリ本体の改造
- SQLite以外のクラウド同期
- 抽選モード
- アイコン表示
- メンバー/モデレーター表示
- アニメーション演出
- カスタムテーマ
- 視聴者人数の可変設定

## 重要仕様

- `next` status は作らない
- `waiting` 上位3件を NEXT として扱う
- `waiting` 4件目以降を QUEUE として扱う
- 「参加者募集中」は表示用プレースホルダーでありDBに保存しない
- 参加回数は `participation_count`
- 参加回数はOBSに表示しない
- 管理画面では参加回数を表示する
- NOWが3人未満で受付中の場合、新規参加希望者はNOWへ直接補充する
