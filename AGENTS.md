# 開発ルール

## 現行方針

- アプリ名称は「参加型整列プラグイン」
- Chrome拡張ではなくローカルWebアプリとして開発する
- OBS表示は `/overlay`
- 管理画面は `/control`
- 設定画面は `/settings`
- 視聴者3人固定
- 状態保存はSQLiteを前提とする
- チャット取得は外部チャット取得アプリケーションから受け取る
- 本アプリは、受信コメントの正規化、参加/取消判定、待機列整理、管理画面、OBS表示を担当する

## 旧実装の扱い

- `extension/` 配下は旧Chrome拡張アプローチの参考資料
- `extension/` 配下を現行実装として扱わない
- 旧実装から参考にしてよいのは、参加回数、参加順、重複処理、表示名安全処理などの設計思想のみ

## MVPで実装しないもの

- Chrome拡張
- YouTubeページへのUI注入
- 配信サイトAPIへの直接接続
- 非公開API利用
- 外部アプリ本体の改造
- SQLite以外のクラウド同期
- 抽選モード
- アイコン表示
- メンバー/モデレーター表示
- アニメーション演出
- カスタムテーマ
- 視聴者人数の可変設定

## 重要仕様

- `next` status は作らない
- `waiting` 上位3件を NEXT として扱う
- `waiting` 4件目以降を QUEUE として扱う
- 「参加者募集中」は表示用プレースホルダーでありDBに保存しない
- 参加回数は `participation_count`
- 参加回数はOBSに表示しない
- 管理画面では参加回数を表示する
- NOWが3人未満で受付中の場合、新規参加希望者はNOWへ直接補充する

## 開発順序の原則

- モック完成後は、まず `mock_state.py` に集中している責務をサービス層へ分離する
- SQLite保存や外部コメント受信は、`QueueService` / `OverlayStateService` の整理後に実装する
- 1つのPRで大きな段階をまたがない
- 仕様書・開発仕様にない大規模変更を勝手に先行しない
- exe化や配布形態の固定は、MVP本実装が固まってから検討する

## Windows初期セットアップ（2026-09-19）

- ユーザー指定によりWindowsデスクトップ方式選定と初期ビルドを実施済み。`docs/WINDOWS_DESKTOP.md` を参照する
- pywebview + FastAPI + SQLite。OBSはローカルHTTPブラウザソースを維持する
- `desktop/` は起動・終了・保存先・多重起動を担当し、待機列ロジックを複製しない
- 完成版配布、公開、マージは初期セットアップに含めない
- AI API依存を追加しない（Astraは開発モデル指定）
- ユーザーの配信環境はOBS + Microsoft Edge。Google Chromeを必須にせず、同時稼働時のメモリ消費を検証する
- 最終配布先はGitHub Releases。Windows用の実行ZIPをダウンロード・展開して起動できることを完成条件に含める
- 最終配布はWindowsの「インストールされているアプリ」から削除できるインストーラーを主とし、ZIPは検証用/補助配布とする
- 完全削除では本アプリ所有の本体・DB・設定・ログ・WebViewキャッシュ・登録情報を削除し、OBS設定・Edgeプロファイル・共有WebView2/.NETは変更しない。具体仕様はWindows文書のアンインストール節を参照

## OBS表示の追加要件（2026-09-19）
- ユーザー指定により受付/見出し文言と幅・高さ・文字サイズを設定可能にする（自由CSSテーマとは別）。
- OBSのタイトル・背景・枠は表示しない。
- 表示する待機合計はNOWを除きNEXTを含む。グループ数は3人で切り上げる。内部のQUEUE末尾人数とは区別する。

- ユーザー追加指定: 管理UIとOBSのフォントは一括・項目別設定に対応。元ファイル/OSフォントを変更せず、選択したフォントを所有DBへコピーする。JSONバックアップは選択設定のみで、本体は別PCに再登録する。

- preview.8: 縦/横配置、名前表示4択、YouTubeハンドルとニックネーム分離に対応。手動編集した申告名を受信で上書きしない。「初回参加優先モード」は名称変更で優先ロジックは維持。

- 名称変更後も内部APP_ID・保存先WaitingListApp・インストーラーAppIdは保持し、既存データと所有台帳を引き継ぐ。表示名・exe・配布名は「参加型整列プラグイン」。

## YouTubeコメント受信（2026-09-20）
ユーザーの明示指示でYouTube公式APIの直接受信を追加。従来の外部入力方式も維持。非公開API・ブラウザ自動操作は使わない。APIキーはメモリのみ、投稿機能は対象外。初回履歴を無視し、ページトークン・API指定間隔・終了時の停止を守る。

## 管理画面の参加者アイコン（2026-09-20）
ユーザー明示指定により待機者一覧にYouTubeプロフィール画像を追加。公式コメントAPIのauthorDetails.profileImageUrlを利用。OBSは文字表示を維持。画像なし・失敗時の代替表示と遅延読込、既存データ移行・バックアップ互換を保つ。

## わんコメ版（2026-09-21）
ユーザー指示で独立版1.1.3と別系統の試作を開発。codex/onecomme-edition、詳細docs/ONECOMME_EDITION.md。
OneComme公式プラグイン経由のコメント受信と専用管理ページを使用する。本体を改造しない。
保存先WaitingListAppOneComme、ポート18765を専用とし、既存版の保存先・Release・タグを更新しない。
明示的なJSONバックアップ復元のみで移行する。専用ワーカーはプラグインが起動・停止を所有する。
プラグイン実機検証前は試作扱いとし、公開版の置換やmainへのマージは行わない。

## 今後の開発対象（ユーザー指定 2026-09-21）
- 特段の指定がない依頼は、わんコメプラグイン版を指す。名称は「待機列整理アプリ」。
- 独立版1.1.3はそのまま維持し、今後の更新対象にしない。
- ユーザーから正式版とダウンロード公開を指示済み。わんコメ版専用のonecomme-v1.0.0以降でリリースする。旧版のタグ・添付・保存先は変更しない。
- 未検証の実配信・OBS実機操作を確認済みと表記しない。管理画面は公式プラグインのURLから開く専用ページ。

## Bot追加（2026-09-21）
- ユーザー指示でBot専用アカウントの通知を追加。初期無効、個別通知設定、NOW案内、本人待機順位、10/30分の参加方法通知。
- 受信はわんコメ、投稿はYouTube公式API。わんコメのログイン情報や非公開APIを流用しない。
- デスクトップOAuthクライアントは利用者が準備する。Bot本人をチャンネルIDで除外し、認証情報はWindows DPAPIで保護する。BOT.md参照。
- 実アカウントの認証・投稿は未確認。Bot追加版1.1.0は検証版として公開し、正式な本体1.0.0も残す。

## 共通Botへの統合（ユーザー指定 2026-09-22）
- 最新のわんコメ版へ共通Botを統合し、ユーザー確認済みの番号0.1.2で公開する。過去のリリース・タグと独立版は変更しない。
- 従来の利用者個別OAuth方式を置換する。コメント取得はわんコメ、待機列・OBSは既存アプリ、投稿とBot OAuthはCloudflare共通バックエンド。
- Google秘密をクライアントへ配布しない。PCはDPAPI保護した端末キーのみ使用する。旧Google認証レコードは使用・送信・自動削除しない。
- 通知は次グループNOW呼出し、@JoinQueueBotへの本人順番返信、15/30分の待機人数・組数案内だけ。初回NOW補充・参加受付・辞退・完了のお礼は不要。
- 3機能を個別チェックボックスで切替・永続保存。UID、既存SQLite・OBS設定を維持する。
- 本番の投稿有効化は別途明示承認が必要。公開と配信者認証受付のみ承認済み。課金設定は変更しない。
